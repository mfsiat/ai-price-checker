# Baked into the production build by `npm run build` (Vercel).
# Local `npm start` ignores this file and uses the localhost fallback in src/api.js.
REACT_APP_API_URL=https://ai-price-checker-mnv9.onrender.com
